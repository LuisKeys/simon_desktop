# Provider API keys (all optional; configure at least one)
OPENAI_API_KEY=
OPENAI_MODEL=

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=

OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=

# auto, or a specific model like gpt-5, claude-3-5-sonnet-latest, ollama
DEFAULT_MODEL=auto

# Embedding provider used to index documents into the knowledge base
EMBEDDING_PROVIDER=
EMBEDDING_MODEL=
